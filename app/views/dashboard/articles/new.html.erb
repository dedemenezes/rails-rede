<div class="container">
  <%= simple_form_for([:dashboard, @article]) do |f| %>
    <%#= f.input :tags, collection: Tag.all, multiple: true, input_html: { data: { controller: 'tom-select' } } %>
    <%= f.input :tag_list, input_html: {value: f.object.tag_list.join(",")} %>
    <%= f.input :banner, as: :file %>
    <%= f.input :header %>
    <%= f.input :sub_header %>
    <%= f.rich_text_area :rich_body %>
    <%= f.submit %>
  <% end %>
</div>
