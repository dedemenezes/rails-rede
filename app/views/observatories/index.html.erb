<div class="container">
  <div class="row justify-content-center mt-2 mb-5">
    <div class="col-12 mt-3">
      <div class="d-flex justify-content-between align-items-center border border-rede-primary border-info border-5 border-end-0 border-top-0 border-bottom-0 ps-4">
        <h1>Observatórios</h1>
      </div>
    </div>
  </div>
</div>
<div class="container">
  <div class="cards-container">
    <div class="cards__observatories">
      <% @observatories.each do |observatory| %>
        <div class="observatory__card">
          <%= display_banner(observatory) %>
          <p><%= link_to observatory.name, observatory_path(observatory) %></p>
        </div>
      <% end %>
    </div>
  </div>

</div>
