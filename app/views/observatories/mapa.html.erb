<% content_for :meta_title, "Mapa de conflitos - #{DEFAULT_META["meta_product_name"]}" %>

<div class="container"
data-controller='scroll-reveal'>
  <%= render 'map', tilesets: @tilesets, hide_filter: true, header: true, tag: :h1, css_classes: { } %>
</div>
